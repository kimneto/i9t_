import 'package:flutter/material.dart';

class LoadingCardCustom extends StatelessWidget {
  const LoadingCardCustom({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Row(
        children: [],
      ),
    );
  }
}
